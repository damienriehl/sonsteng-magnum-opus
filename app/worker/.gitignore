# Local-only secrets loaded by `wrangler dev` — NEVER commit.
.dev.vars
.dev.vars.*

# Wrangler + node build/runtime artifacts.
.wrangler/
node_modules/
dist/

# Generated editor bundles copied in by scripts/bundle-editor-data.mjs (never commit).
editor-data/
