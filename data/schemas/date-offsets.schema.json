{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sonsteng.damienriehl.com/spine/schemas/date-offsets.schema.json",
  "title": "Matter prose Day Zero offsets",
  "version": "1.0.0",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "matter_id", "anchor", "entries"],
  "properties": {
    "schema_version": { "const": "1.0.0" },
    "matter_id": { "type": "string", "pattern": "^m\\d{2}$" },
    "anchor": { "type": "string", "format": "date" },
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["source", "locator", "literal", "day_zero_offset"],
        "oneOf": [
          { "required": ["block_id"], "not": { "required": ["durable_locator"] } },
          { "required": ["durable_locator"], "not": { "required": ["block_id"] } }
        ],
        "properties": {
          "source": { "type": "string" },
          "block_id": { "type": "string", "pattern": "^b:[0-9a-f]{8}$" },
          "locator": { "type": "integer", "minimum": 0 },
          "literal": { "type": "string" },
          "day_zero_offset": { "type": "integer" },
          "durable_locator": {
            "type": "string",
            "pattern": "^(?:raw:[0-9a-f]{16}:occurrence:[1-9][0-9]*:date:[0-9]+|json:.+:date:[0-9]+)$"
          }
        }
      }
    }
  }
}
